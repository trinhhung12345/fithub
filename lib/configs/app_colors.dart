import 'package:flutter/material.dart';

class AppColors {
  // Màu chính (Brand Colors)
  static const Color primary = Color(0xFFF64400); // Cam đậm
  static const Color secondary = Color(0xFFD96F32); // Cam nhạt/Nâu

  // Màu nền & Text
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
  static const Color inputBackground = Color(
    0xFFF3F3F3,
  ); // Màu xám nhạt ô input
  static const Color hintText = Color(0xFF9E9E9E);
}
